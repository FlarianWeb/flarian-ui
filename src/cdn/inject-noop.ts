/**
 * Заглушка `@flarian/ui/inject` для CDN iife-бандла.
 * В CDN-режиме нет сборки: стили подключаются `<link>` (flarian-ui.iife.css + theme.css),
 * конфиг и спрайт — вручную через `FlarianUI.provideConfig()` / `FlarianUI.provideSprite()`.
 */
export {};
