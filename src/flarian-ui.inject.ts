/**
 * Точка инъекции конфига и стилей — `@flarian/ui/inject`.
 *
 * Этот исходник — placeholder для dev-режима внутри пакета: плагин подменяет его
 * содержимое в `load`-хуке сгенерированным кодом (CSS + `provideConfig` + `provideSprite`).
 *
 * Собранный вариант `dist/flarian-ui.inject.js` генерируется при `vite build` и содержит
 * дефолтные импорты (styles.css, theme.css, статичный спрайт) — он выполняется у
 * потребителя, который не подключил плагин.
 *
 * Файл резолвится по уникальному имени `flarian-ui.inject` — не переименовывать.
 */
export {};
