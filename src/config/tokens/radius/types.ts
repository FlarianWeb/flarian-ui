import type { UiRadius } from '~/tokens';

/**
 * Конфигурация радиусов скругления.
 * Ключи типизированы моделью `UiRadius`. → `--ui-radius-*`
 */
export type DefaultRadiusConfig = Record<UiRadius, string>;
